---
sidebar_position: 5
---

import React from "react"
import TreeSpider from 'treespider';

# Trees

These are the different tree choices that can be chosen from, they will be caterized into radial and non-radial trees:  
  
## Radial trees
  
The following are the radial or circular trees
  
### goldenRod

The goldenRod tree type is one of the radial tree types, example
  
```ts
const instance1 = new TreeSpider({
    targetContainer: "#container-1",
    tree_type: "goldenRod"
})
```

if you run this it would look something like this:  
  
export const TSpydExample1 = React.memo(() => {
    React.useLayoutEffect(() => {
        const inst1 = new TreeSpider({
            targetContainer: "#spyd-tr-1",
            random_data_length: 50,
            width: '100%',
            tree_type: "goldenRod"
        })
    }, [])
    
    return (
        <div id="spyd-tr-1" style={{marginBottom: 25}}></div>
    )
})

<TSpydExample1 />

### radialSpiderLeg

The radialSpiderLeg tree type is the second radial type tree types, example

```ts
const instance1 = new TreeSpider({
    targetContainer: "#container-1",
    tree_type: "radialSpiderLeg"
})
```

if you run this it would look something like this:  
  
export const TSpydExample2 = React.memo(() => {
    React.useLayoutEffect(() => {
        const inst1 = new TreeSpider({
            targetContainer: "#spyd-tr-2",
            random_data_length: 50,
            width: '100%',
            tree_type: "radialSpiderLeg"
        })
    }, [])
    
    return (
        <div id="spyd-tr-2" style={{marginBottom: 25}}></div>
    )
})

<TSpydExample2 />

## Non-radial trees

The following are the non-radial trees types.  

### default

This is the default tree type, and it is also useful in but not limited to organizations with hierarchical type structure, example

```ts
const instance1 = new TreeSpider({
    targetContainer: "#container-1",
    tree_type: "default"
})
```

export const TSpydExample3 = React.memo(() => {
    React.useLayoutEffect(() => {
        const inst1 = new TreeSpider({
            targetContainer: "#spyd-tr-3",
            random_data_length: 50,
            width: '100%',
            tree_type: "default"
        })
    }, [])
    
    return (
        <div id="spyd-tr-3" style={{marginBottom: 25}}></div>
    )
})

<TSpydExample3 />

### cellar

The cellar tree structure is also useful in but not limited to organizations with hierarchical type structure, example

```ts
const instance1 = new TreeSpider({
    targetContainer: "#container-1",
    tree_type: "cellar"
})
```

export const TSpydExample4 = React.memo(() => {
    React.useLayoutEffect(() => {
        const inst1 = new TreeSpider({
            targetContainer: "#spyd-tr-4",
            random_data_length: 50,
            width: '100%',
            tree_type: "cellar"
        })
    }, [])
    
    return (
        <div id="spyd-tr-4" style={{marginBottom: 25}}></div>
    )
})

<TSpydExample4 />

### hSpider

The hSpider tree is an horizontal tree, example

```ts
const instance1 = new TreeSpider({
    targetContainer: "#container-1",
    tree_type: "hSpider"
})
```

export const TSpydExample5 = React.memo(() => {
    React.useLayoutEffect(() => {
        const inst1 = new TreeSpider({
            targetContainer: "#spyd-tr-5",
            random_data_length: 50,
            width: '100%',
            tree_type: "hSpider"
        })
    }, [])
    
    return (
        <div id="spyd-tr-5" style={{marginBottom: 25}}></div>
    )
})

<TSpydExample5 />

### hSpiderWalk

The hSpiderWalk tree is an horizontal tree just like the hSpider but it is in the shape of the letter "H", example

```ts
const instance1 = new TreeSpider({
    targetContainer: "#container-1",
    tree_type: "hSpiderWalk"
})
```

export const TSpydExample6 = React.memo(() => {
    React.useLayoutEffect(() => {
        const inst1 = new TreeSpider({
            targetContainer: "#spyd-tr-6",
            random_data_length: 50,
            width: '100%',
            tree_type: "hSpiderWalk"
        })
    }, [])
    
    return (
        <div id="spyd-tr-6" style={{marginBottom: 25}}></div>
    )
})

<TSpydExample6 />

### vSpiderWalk

The vSpiderWalk tree is a verticaal tree just like the hSpiderWalk but it is vertical, example

```ts
const instance1 = new TreeSpider({
    targetContainer: "#container-1",
    tree_type: "vSpiderWalk"
})
```

export const TSpydExample7 = React.memo(() => {
    React.useLayoutEffect(() => {
        const inst1 = new TreeSpider({
            targetContainer: "#spyd-tr-7",
            random_data_length: 50,
            width: '100%',
            tree_type: "vSpiderWalk"
        })
    }, [])
    
    return (
        <div id="spyd-tr-7" style={{marginBottom: 25}}></div>
    )
})

<TSpydExample7 />
